<% default_classes = "flex size-4 cursor-pointer appearance-none items-center justify-center " \
                     "rounded-full border-2 border-secondary transition-all " \
                     "before:size-0 before:rounded-full before:bg-accent " \
                     "checked:border-accent checked:before:size-2 " \
                     "motion-safe:before:transition-all" %>
<% options = args.fourth || local_assigns.except(:args) %>
<% options[:class] = class_names(default_classes, options[:class]) %>
<% args[3] = options %>

<%= radio_button *args %>
