<% @current_name = local_assigns.delete(:name) || use_id if local_assigns.delete(:multiple).blank?  %>
<% data = data_attributes({
  data: {
    controller: "ui--accordion",
    action:  "keydown.up->ui--accordion#focusPrevious keydown.down->ui--accordion#focusNext " \
              "keydown.home->ui--accordion#focusFirst keydown.end->ui--accordion#focusLast",
    }
  }, local_assigns) %>

<%= tag.div(class: local_assigns.delete(:class), data:, **local_assigns.except(:data)) do %>
  <%= yield %>
<% end %>
