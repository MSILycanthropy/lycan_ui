---
title: Select
component: select
---

<% content_for :description do %>
  <p>
    A styled wrapper around the native HTML select element.
  </p>

  <div class="pt-4">
    <%= render 'partials/example', wrapper_classes: 'flex justify-center' %>
  </div>
<% end %>

<% content_for :usage do %>
  <div class="space-y-3">
    <div>
      <h4 id="forms" class="text-lg scroll-m-20 pb-2">
        Usage in forms
      </h4>

      <p class="text-sm">
        To use directly with Rails forms, please see

        <%= link_to '/forms', class: 'underline underline-offset-4 decoration-accent hover:decoration-2' do %>
          Installing form helpers
        <% end %>
      </p>

      <%= render 'partials/example', path: "select/form", one: true,  wrapper_classes: 'flex justify-center' %>
    </div>
  </div>
<% end %>

<% content_for :usage_links do %>
  <li>
    <a class="text-sm underline-offset-4 hover:underline decoration-accent transition-all" href="#form">Usage in forms</a>
  </li>
<% end %>

<% content_for :accessibility do %>
  <p>
    Utilizes the

    <%= external_link_to 'https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/select' do %>
      <span> HTML Select element</span>
    <% end %>
  </p>
<% end %>

<%= render 'partials/doc' %>
